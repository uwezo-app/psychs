import React from "react";

const WebSocketsProvider = (props: {}) => {
  const {} = props;

  return (
    <div>
      <h1>WebSocketsProvider</h1>
    </div>
  );
};

export default WebSocketsProvider;
